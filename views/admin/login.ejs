<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login Nagari - Travel Platform</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #6a85b6 0%, #bac8e0 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .login-container {
      background: white;
      border-radius: 20px;
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
      overflow: hidden;
      max-width: 800px;
      width: 100%;
      height: 90vh;
      display: flex;
      position: relative;
      transition: box-shadow 0.3s;
    }

    .login-container:hover {
      box-shadow: 0 35px 70px rgba(0,0,0,0.20);
    }

    .left-section2 {
      flex: 1;
      background-image: url('https://1001indonesia.net/asset/2019/10/Danau-Singkarak.jpg');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: flex-start;
      padding: 30px;
      color: white;
      position: relative;
      min-width: 220px;
    }

    .left-section2::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0; bottom: 0;
      background: rgba(0, 0, 0, 0.4);
      z-index: 1;
    }

    .left-content {
      position: relative;
      z-index: 2;
      width: 100%;
      text-align: left;
      animation: fadeInLeft 1s;
    }

    @keyframes fadeInLeft {
      from { opacity: 0; transform: translateX(-40px);}
      to { opacity: 1; transform: translateX(0);}
    }

    .main-title {
      font-size: 26px;
      font-weight: 600;
      line-height: 1.3;
      margin-bottom: 10px;
    }

    .welcome-text, .subtitle {
      font-size: 14px;
      font-weight: 300;
      opacity: 0.9;
      margin-bottom: 10px;
    }

    .right-section {
      flex: 1;
      padding: 40px 30px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      background: white;
      overflow-y: auto;
      min-width: 220px;
      animation: fadeInRight 1s;
    }

    @keyframes fadeInRight {
      from { opacity: 0; transform: translateX(40px);}
      to { opacity: 1; transform: translateX(0);}
    }

    .form-header {
      text-align: center;
      margin-bottom: 30px;
    }

    .welcome-back {
      font-size: 20px;
      font-weight: 600;
      color: #333;
      margin-bottom: 8px;
    }

    .form-group {
      margin-bottom: 18px;
    }

    .form-group label {
      display: block;
      margin-bottom: 6px;
      font-size: 13px;
      font-weight: 500;
      color: #333;
    }

    .form-group input {
      width: 100%;
      padding: 14px;
      border: 1.5px solid #ccc;
      border-radius: 8px;
      background: #f9f9f9;
      font-size: 15px;
      transition: border-color 0.2s, box-shadow 0.2s;
      outline: none;
    }

    .form-group input:focus {
      border-color: #667eea;
      box-shadow: 0 0 0 2px #667eea33;
      background: #fff;
    }

    .login-button {
      width: 100%;
      padding: 14px;
      background: linear-gradient(90deg, #667eea 60%, #764ba2 100%);
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 15px;
      font-weight: bold;
      cursor: pointer;
      transition: background 0.2s, transform 0.1s;
      margin-top: 10px;
      box-shadow: 0 2px 8px #667eea22;
    }

    .login-button:hover, .login-button:focus {
      background: linear-gradient(90deg, #5a67d8 60%, #5f3b8a 100%);
      transform: translateY(-2px) scale(1.03);
    }

    .error-message {
      background: #fee;
      color: #e74c3c;
      padding: 12px;
      border-radius: 8px;
      margin-bottom: 20px;
      text-align: center;
      font-size: 13px;
      animation: shake 0.3s;
    }

    @keyframes shake {
      0% { transform: translateX(0);}
      25% { transform: translateX(-5px);}
      50% { transform: translateX(5px);}
      75% { transform: translateX(-5px);}
      100% { transform: translateX(0);}
    }

    @media (max-width: 900px) {
      .login-container {
        max-width: 95vw;
        height: auto;
      }
      .left-section2, .right-section {
        min-width: 180px;
      }
    }

    @media (max-width: 768px) {
      .login-container {
        flex-direction: column;
        height: auto;
        min-height: 100vh;
      }
      .left-section2, .right-section {
        width: 100%;
        padding: 30px 18px;
      }
      .left-content {
        text-align: center;
      }
    }

    @media (max-width: 480px) {
      .login-container {
        border-radius: 0;
        box-shadow: none;
      }
      .left-section2, .right-section {
        padding: 18px 8px;
      }
      .main-title {
        font-size: 28px !important;
      }
      .welcome-text {
        font-size: 18px !important;
      }
      .subtitle {
        font-size: 14px !important;
      }
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="left-section2">
      <div class="left-content">
        <div class="welcome-text" style="font-size: 24px; font-weight: bold;">X KOTO SINGKARAK</div>
        <div class="main-title" style="font-size: 40px; font-weight: bold;">NAGARI TIKALAK</div>
        <div class="subtitle" style="font-size: 18px; margin-top: 10px;">Manajemen Data dan Informasi Nagari</div>
      </div>
    </div>
    <div class="right-section">
      <div class="form-header">
        <div class="welcome-back">WELCOME BACK!</div>
      </div>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="error-message"><%= error %></div>
      <% } %>

      <form method="POST" action="/admin/login" autocomplete="on">
        <div class="form-group">
          <label for="username">Username</label>
          <input id="username" name="username" type="text" required placeholder="Enter username" autocomplete="username">
        </div>
        <div class="form-group">
          <label for="password">Password</label>
          <input id="password" name="password" type="password" required placeholder="Enter password" autocomplete="current-password">
        </div>
        <button type="submit" class="login-button">Sign In</button>
      </form>
    </div>
  </div>
</body>
</html>
